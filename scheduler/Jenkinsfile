pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile.jenkins'
            dir 'scheduler'
        }
    }
    stages {
        stage('test') {
            steps {
                dir("scheduler/app") {
                    sh 'python run_tests.py'
                }
            }
        }
    }
}