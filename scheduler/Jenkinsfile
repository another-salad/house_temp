pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile.jenkins'
            dir 'scheduler'
        }
    }
    stages {
        stage('test') {
            steps {
                dir("scheduler") {
                    sh 'python run_tests.py'
                }
            }
        }
    }
}